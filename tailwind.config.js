/** @type {import('tailwindcss').Config} */
module.exports = {
    darkMode: 'class', // This is REQUIRED for manual toggle
    content: [
      './app/**/*.{js,jsx}',
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }